# handframe-character-video
只需用户上传一条原片（人物视频），先明确选择一个或多个原创角色/描述性画风，再通过视频编辑模型生成动作、构图和全局时序对齐的完整角色转绘视频；支持在成片手势框内约每 1–2 秒轮换一个显著不同的角色，切换时读取各转绘片的同一全局时刻。使用 MediaPipe 检测双手四角，把手部四边形作为动态遮罩窗口，仅在框内显示同位置风格化画面。用户提到现有 IP、艺术家或工作室时，只转译为原创原型或一般视觉特征。不得根据外表推断性别，不调用摄像头，不把本地滤镜冒充角色转绘。
